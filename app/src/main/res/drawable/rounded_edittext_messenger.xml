<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/light_back" /> <!-- Màu nền của EditText -->
    <corners android:radius="16dp" /> <!-- Bán kính góc bo -->
    <stroke android:width="2dp" android:color="@color/light_back1" /> <!-- Đường viền (nếu cần) -->
</shape>